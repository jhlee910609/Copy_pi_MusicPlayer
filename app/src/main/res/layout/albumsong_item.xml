<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="70dp"
    android:background="@color/white"
    android:orientation="horizontal">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/artist_img"
        android:layout_toRightOf="@+id/artist_img"
        android:gravity="center">

        <ImageView
            android:id="@+id/albumSong_img"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            app:srcCompat="@mipmap/ic_launcher" />

        <TextView
            android:id="@+id/albumSong_title"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:gravity="center"
            android:text="title"
            android:textAlignment="center"
            android:textSize="18sp"
            android:layout_toRightOf="@+id/albumSong_img"
            android:layout_toEndOf="@+id/albumSong_img" />

        <TextView
            android:id="@+id/albumSong_artist"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:gravity="center_horizontal"
            android:text="artist"
            android:textAlignment="center"
            android:layout_below="@+id/albumSong_title"
            android:layout_toRightOf="@+id/albumSong_img"
            android:layout_toEndOf="@+id/albumSong_img" />

    </RelativeLayout>


</RelativeLayout>
